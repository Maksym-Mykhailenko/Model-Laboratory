{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the local Model Laboratory window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
