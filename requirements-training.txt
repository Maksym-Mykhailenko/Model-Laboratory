# Optional Stage-7 QLoRA bootstrap. Use CPython >=3.10,<3.14, then freeze and verify
# the complete transitive installation with freeze_interpreter_training_environment.py.
# Keep this environment and all ML dependencies out of the desktop sidecar.
-r requirements.txt
torch==2.8.0
transformers==5.15.1
accelerate==1.12.0
peft==0.20.0
bitsandbytes==0.50.1
huggingface-hub==1.5.0
safetensors==0.8.0
packaging==26.0
